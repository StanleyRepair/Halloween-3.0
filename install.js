(()=>{
const ua=navigator.userAgent||'';
const standalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;
const isChrome=/Chrome\/\d+/i.test(ua)&&!/EdgA|EdgiOS|OPR|SamsungBrowser|FBAN|FBAV|FB_IAB|Messenger|Instagram/i.test(ua);
const isAndroid=/Android/i.test(ua);
const installCard=document.getElementById('chromeInstallCard'),browserCard=document.getElementById('openChromeCard'),installedCard=document.getElementById('installedCard'),installBtn=document.getElementById('installNowButton'),chromeBtn=document.getElementById('openChromeButton'),openAppBtn=document.getElementById('openInstalledAppButton'),fallbackBtn=document.getElementById('showInstallInstead'),status=document.getElementById('installStatus'),installedStatus=document.getElementById('installedStatus'),installedTitle=document.getElementById('installedTitle'),installedText=document.getElementById('installedText');
let deferredPrompt=null,waiters=[];
function show(el){[installCard,browserCard,installedCard].forEach(x=>x.hidden=x!==el)}
function showInstalled(certain=true){installedTitle.textContent=certain?'Aplikacja jest zainstalowana':'Aplikacja prawdopodobnie jest już zainstalowana';installedText.textContent=certain?'Uruchom Halloween 3.0 jako zainstalowaną aplikację.':'Chrome nie udostępnia instalatora. Spróbuj uruchomić już zainstalowaną aplikację.';installedStatus.textContent='Kliknij „Otwórz aplikację”. Android uruchomi ją przez dedykowany protokół PWA.';show(installedCard)}
function resolvePrompt(){waiters.splice(0).forEach(r=>r())}
async function isInstalled(){if(standalone)return true;try{if(typeof navigator.getInstalledRelatedApps==='function'){const apps=await navigator.getInstalledRelatedApps();if(Array.isArray(apps)&&apps.some(a=>a.platform==='webapp'))return true}}catch(e){console.warn('Installed app detection failed',e)}return false}
function openInstalledApp(){window.H3Analytics?.track?.('install_open_app_click',{android:isAndroid,method:'protocol'});if(!isAndroid){location.href='../';return}let left=false;const onVisibility=()=>{if(document.hidden)left=true};document.addEventListener('visibilitychange',onVisibility);installedStatus.textContent='Uruchamiam Halloween 3.0…';try{location.href='web+halloween3:open'}catch(e){console.warn(e)}setTimeout(()=>{document.removeEventListener('visibilitychange',onVisibility);if(!left&&!document.hidden)installedStatus.textContent='Aplikacja nie przejęła linku. Otwórz ją raz ręcznie z ekranu telefonu, aby Android zarejestrował nowy sposób uruchamiania.'},1800)}
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;resolvePrompt();if(isChrome&&!standalone)show(installCard)});
window.addEventListener('appinstalled',()=>{deferredPrompt=null;setTimeout(()=>showInstalled(true),500);window.H3Analytics?.track?.('install_landing_installed',{source:'install_page'})});
async function waitForPrompt(ms=4200){if(deferredPrompt)return true;await new Promise(resolve=>{let done=false;const finish=()=>{if(done)return;done=true;resolve()};waiters.push(finish);setTimeout(finish,ms)});return !!deferredPrompt}
async function install(){installBtn.disabled=true;installBtn.textContent='URUCHAMIANIE INSTALATORA…';status.textContent='Przygotowuję instalację…';window.H3Analytics?.track?.('install_landing_click',{browser:'chrome'});try{if(await isInstalled()){showInstalled(true);return}const ready=await waitForPrompt();if(!ready){if(await isInstalled())showInstalled(true);else showInstalled(false);return}const prompt=deferredPrompt;deferredPrompt=null;await prompt.prompt();const result=await prompt.userChoice;if(result?.outcome==='accepted'){status.textContent='Instalowanie aplikacji…';installBtn.textContent='INSTALOWANIE…';setTimeout(()=>showInstalled(true),1200)}else{status.textContent='Instalacja została anulowana.';installBtn.textContent='ZAINSTALUJ APLIKACJĘ'}}catch(e){console.warn(e);status.textContent='Nie udało się uruchomić instalatora Chrome.';installBtn.textContent='ZAINSTALUJ APLIKACJĘ'}finally{installBtn.disabled=false}}
function openChrome(){window.H3Analytics?.track?.('install_open_chrome_click',{source:/FBAN|FBAV|FB_IAB|Messenger/i.test(ua)?'messenger':'other_browser'});if(isAndroid){const target=location.host+location.pathname+location.search;location.href=`intent://${target}#Intent;scheme=https;package=com.android.chrome;S.browser_fallback_url=${encodeURIComponent(location.href)};end`;return}navigator.clipboard?.writeText(location.href).catch(()=>{});chromeBtn.textContent='LINK SKOPIOWANY';setTimeout(()=>chromeBtn.textContent='OTWÓRZ W CHROME',1800)}
installBtn?.addEventListener('click',install);chromeBtn?.addEventListener('click',openChrome);openAppBtn?.addEventListener('click',openInstalledApp);fallbackBtn?.addEventListener('click',()=>{show(installCard);installBtn.textContent='ZAINSTALUJ APLIKACJĘ';status.textContent='Kliknij instalację. Jeśli Chrome jej nie pokaże, użyj menu ⋮ i opcji „Zainstaluj aplikację”.'});
(async()=>{if(await isInstalled()){showInstalled(true);window.H3Analytics?.track?.('install_landing_open',{browser:isChrome?'chrome':'other',android:isAndroid,installed:true});return}if(isChrome){show(installCard);setTimeout(async()=>{if(!deferredPrompt&&await isInstalled())showInstalled(true)},2200)}else show(browserCard);window.H3Analytics?.track?.('install_landing_open',{browser:isChrome?'chrome':'other',android:isAndroid,installed:false})})();
})();