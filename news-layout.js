(()=>{const section=document.querySelector('[data-view="news"]');if(!section)return;const LONG_TEXT_MIN=180;function reflow(card){if(!card.classList.contains('news-managed'))return;const withImage=card.classList.contains('news-with-image');let copy=card.querySelector(':scope > .news-post-copy');let icon=card.querySelector(':scope > .card-icon');if(!copy&&!withImage){copy=[...card.children].find(el=>el.tagName==='DIV'&&!el.classList.contains('card-icon')&&!el.classList.contains('news-layout-head'))||null}if(!copy&&withImage){const head=card.querySelector(':scope > .news-post-head');copy=head?.querySelector('.news-post-copy')||card.querySelector(':scope > .news-post-copy')}if(!copy)return;const p=copy.querySelector(':scope > p');const textLength=(p?.textContent||'').trim().length;const isLong=textLength>=LONG_TEXT_MIN;if(!withImage&&!isLong){card.classList.remove('news-long-post');return}card.classList.toggle('news-long-post',isLong);let head=card.querySelector(':scope > .news-layout-head, :scope > .news-post-head');if(!head&&!withImage&&icon){head=document.createElement('div');head.className='news-layout-head';card.insertBefore(head,icon);head.append(icon,copy)}if(p&&!p.classList.contains('news-full-text')){p.classList.add('news-full-text');const img=card.querySelector(':scope > .news-post-image');card.insertBefore(p,img||null)}const full=card.querySelector(':scope > .news-full-text');if(full)full.classList.toggle('news-long-text',isLong);const btn=copy.querySelector(':scope > .text-action');if(btn){const img=card.querySelector(':scope > .news-post-image');card.insertBefore(btn,img||null)}}function apply(){section.querySelectorAll(':scope > article.content-card.news-managed').forEach(reflow)}new MutationObserver(()=>requestAnimationFrame(apply)).observe(section,{childList:true,subtree:true});document.querySelector('.nav-item[data-tab="news"]')?.addEventListener('click',()=>setTimeout(apply,80));setTimeout(apply,250)})();