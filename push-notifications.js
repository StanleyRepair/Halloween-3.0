(()=>{
const VAPID_PUBLIC='BJg6Yy6HH6CduNZwTLbtqrqfvjr73AnU-BdLB2kPlRDIS85_W0h_xqiP84lKEqfXC7AM5Vq5DBxmM2d-MvUt3_w';
function b64ToU8(base64){const pad='='.repeat((4-base64.length%4)%4),s=(base64+pad).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(s);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
function canPush(){return 'serviceWorker'in navigator&&'PushManager'in window&&'Notification'in window}
async function saveSubscription(sub){const json=sub.toJSON();const{error}=await sb.rpc('save_push_subscription',{p_endpoint:json.endpoint,p_p256dh:json.keys?.p256dh||'',p_auth:json.keys?.auth||'',p_user_agent:navigator.userAgent});if(error)throw error}
async function subscribe(btn,card){btn.disabled=true;try{if(Notification.permission==='denied'){card.querySelector('small').textContent='Powiadomienia są zablokowane w ustawieniach przeglądarki.';card.hidden=false;return}const permission=await Notification.requestPermission();if(permission!=='granted'){card.querySelector('small').textContent='Bez zgody nie będziemy wysyłać powiadomień.';card.hidden=false;return}const reg=await navigator.serviceWorker.ready;let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64ToU8(VAPID_PUBLIC)});await saveSubscription(sub);localStorage.setItem('h3_push_enabled','1');card.hidden=true}catch(e){console.warn('Push subscribe failed',e);card.querySelector('small').textContent='Nie udało się włączyć powiadomień na tym urządzeniu.';card.hidden=false}finally{btn.disabled=false}}
async function init(){const news=document.querySelector('[data-view="news"]');if(!news||!canPush())return;const existing=document.getElementById('pushOptin');if(existing)return;let remembered=false;try{remembered=localStorage.getItem('h3_push_enabled')==='1'}catch{}const card=document.createElement('div');card.id='pushOptin';card.className='push-optin';card.hidden=true;card.innerHTML='<div class="push-optin-icon">🔔</div><div class="push-optin-copy"><strong>Włącz powiadomienia</strong><small>Dostaniesz ważne aktualności nawet gdy aplikacja jest zamknięta.</small></div><button type="button" class="push-optin-btn">Włącz</button>';const heading=news.querySelector('.section-heading');heading?.insertAdjacentElement('afterend',card);const btn=card.querySelector('.push-optin-btn');btn.addEventListener('click',()=>subscribe(btn,card));try{if(remembered&&Notification.permission==='granted'){const reg=await navigator.serviceWorker.ready;const sub=await reg.pushManager.getSubscription();if(sub){saveSubscription(sub).catch(console.warn);return}}const reg=await navigator.serviceWorker.ready;const sub=await reg.pushManager.getSubscription();if(sub){await saveSubscription(sub);localStorage.setItem('h3_push_enabled','1');return}try{localStorage.removeItem('h3_push_enabled')}catch{}if(Notification.permission==='denied'){card.querySelector('small').textContent='Powiadomienia są zablokowane w ustawieniach przeglądarki.';btn.disabled=true}card.hidden=false}catch(e){console.warn(e);if(!remembered)card.hidden=false}}
window.addEventListener('load',()=>setTimeout(init,300));
})();