(()=>{
function makeInput({multiple=false,onFiles}){const input=document.createElement('input');input.type='file';input.accept='image/*';input.hidden=true;if(multiple)input.multiple=true;input.addEventListener('change',()=>{const files=[...input.files];if(files.length)onFiles(files);input.value=''});return input}
function addContestGooglePhotos(){const actions=document.querySelector('#entryPanel .photo-actions');if(!actions||actions.querySelector('[data-google-photos-contest]'))return;const label=document.createElement('label');label.className='upload-action';label.dataset.googlePhotosContest='1';label.title='Na Androidzie 13+ systemowy selektor może pokazywać również zdjęcia zapisane tylko w Google Photos.';label.textContent='☁️ Google Photos';const input=makeInput({onFiles:files=>{if(typeof window.choosePhoto==='function')window.choosePhoto(files[0])}});label.appendChild(input);actions.appendChild(label)}
function decorateAdminGallery(){const root=document.getElementById('galleryTiles');if(!root)return;root.querySelectorAll('.gallery-admin-tile').forEach(tile=>{if(tile.querySelector('[data-google-photos-admin]'))return;const original=tile.querySelector('input[data-upload]');if(!original)return;const tileId=original.dataset.upload;const label=document.createElement('label');label.className='gallery-upload';label.dataset.googlePhotosAdmin='1';label.title='Na Androidzie 13+ możesz wybrać także zdjęcia zapisane tylko w Google Photos, jeśli Google Photos jest ustawione jako dostawca multimediów w selektorze systemowym.';label.textContent='☁️ Google Photos';const input=makeInput({multiple:true,onFiles:files=>{if(typeof window.uploadGalleryPhotos==='function')window.uploadGalleryPhotos(tileId,files)}});label.appendChild(input);original.closest('.gallery-upload')?.insertAdjacentElement('afterend',label)})}
addContestGooglePhotos();
const galleryRoot=document.getElementById('galleryTiles');if(galleryRoot){decorateAdminGallery();new MutationObserver(()=>decorateAdminGallery()).observe(galleryRoot,{childList:true,subtree:true})}
})();