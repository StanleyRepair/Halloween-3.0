(()=>{const trigger=document.getElementById('hiddenAdminTrigger');if(!trigger)return;let taps=0,lastTap=0;trigger.style.touchAction='manipulation';trigger.style.cursor='default';const onTap=e=>{const now=Date.now();if(now-lastTap>3000)taps=0;lastTap=now;taps+=1;if(taps>=5){taps=0;window.location.assign('./admin.html')}};trigger.addEventListener('pointerup',onTap,{passive:true});})();